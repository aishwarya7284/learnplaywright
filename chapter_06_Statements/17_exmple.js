let userName = "abc";
let password = "abc123";
let isAccountLocked = true;

if (userName === "abc" && password === "abc123" && !isAccountLocked) {
    console.log("user allowed to enter")
} else {
    console.log("user not allowed to enter")
}

